/**
 * apps/web/src/types/api.d.ts
 *
 * ⚠️ 자동 생성 파일 — 직접 편집하지 마세요.
 *
 * 재생성 방법:
 *   1. wrangler dev 실행: pnpm -F @nihongo-n3/api dev
 *   2. 타입 생성: pnpm -F @nihongo-n3/web gen:api-types
 *
 * 이 파일은 openapi-fetch 클라이언트(`typedApi`)의 타입 기반입니다.
 * API가 변경될 때마다 재생성하여 컴파일 시점에 타입 오류를 확인하세요.
 */

export type paths = Record<string, never>;
export type webhooks = Record<string, never>;
export type components = {
  schemas: Record<string, never>;
  responses: Record<string, never>;
  parameters: Record<string, never>;
  requestBodies: Record<string, never>;
  headers: Record<string, never>;
  pathItems: Record<string, never>;
};
export type $defs = Record<string, never>;
export type operations = Record<string, never>;
